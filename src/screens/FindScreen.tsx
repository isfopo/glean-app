import { Text, View } from 'react-native';
const FindScreen = () => {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="text-xl font-serif text-brand-primary">Find Screen</Text>
    </View>
  );
};

export default FindScreen;
