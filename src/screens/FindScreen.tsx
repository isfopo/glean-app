import { Text, View } from 'react-native';
const FindScreen = () => {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="text-xl font-bold text-blue-500">Find Screen</Text>
    </View>
  );
};

export default FindScreen;
